<?php
/* 
 * App Controller
 */
class AppController extends Controller {

/*
	public function __construct() {
        $login = "'".$_SERVER['REMOTE_USER']."'";
		$sql = "SELECT name FROM hour_locations WHERE login = $login";
		$this->loadModel('HourLocations');
		$result = $this->HourLocations->query($sql);	
		if(isset($result[0]['hour_locations']['name'])) {
			$this->set('location_name',$result[0]['hour_locations']['name']);
		}

        // call parent constructor
        //parent :: __constructor();
    }
*/

   
}

?>
